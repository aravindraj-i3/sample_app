<header class="navbar navbar-expand-lg navbar-dark bg-dark fixed-top">
  <div class="container">
    <%# Link the logo to the home page %>
    <%= link_to "sample app" , root_path, id: "logo", class: "navbar-brand" %> <%# FIX: root_path %>
    <nav class="collapse navbar-collapse" id="navbarNav">
      <ul class="navbar-nav ms-auto">
        <li class="nav-item"> <%= link_to "Home" , root_path, class: "nav-link" %> </li> <%# FIX: root_path %>
        <li class="nav-item"> <%= link_to "Help" , help_path, class: "nav-link" %> </li> <%# FIX: help_path %>
        <li class="nav-item"> <%= link_to "Log in" , login_path, class: "nav-link" %> </li> <%# FIX: login_path %>
        <% if logged_in? %>
          <li class="nav-item"> <%= link_to "Users" , users_path, class: "nav-link" %> </li> <%# FIX: users_path (assuming you want a users list) %>
        <% end %>
      </ul>
    </nav>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
  </div>
</header>